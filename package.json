{
	"name": "wrangler-rpc-platform-proxy",
	"workspaces": [
		"./rpc-worker",
		"./consumer"
	],
	"scripts": {
		"dev": "npm run dev --workspace=rpc-worker",
		"consume": "npm run consume --workspace=consumer"
	}
}
